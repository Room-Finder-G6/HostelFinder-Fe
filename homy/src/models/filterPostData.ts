export interface FilterPostData {
    province?: string,
    district?: string,
    commune?: string,
    minSize?: number,
    maxSize?: number,
    minPrice?: number,
    maxPrice?: number,
    roomType?: number
}